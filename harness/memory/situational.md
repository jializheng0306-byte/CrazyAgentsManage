# Situational Memory

- CrazyAgentsManage uses Python/Flask as the current execution substrate.
- Hermes-Agent remains the external runtime/data source.
- `.omx/` is runtime-local only.
- `docs/` and `harness/` are the canonical durable collaboration surfaces.
- HermesAgent is the operations lane, not a second coding lane.

